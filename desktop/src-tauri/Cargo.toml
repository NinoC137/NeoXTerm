[package]
name = "neoxterm-desktop"
version = "0.1.0"
description = "Desktop workbench for NeoXTerm devices"
edition = "2021"
rust-version = "1.77.2"

[build-dependencies]
tauri-build = { version = "2.5.0", features = [] }

[dependencies]
ferry = { package = "neoxterm", path = "../.." }
serde = { version = "1", features = ["derive"] }
tauri = { version = "2.10.0", features = [] }
